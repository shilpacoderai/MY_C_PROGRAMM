#include <stdio.h>

int main() {
    int n=10, sum = 0;

     for (int i = 1; i <= 10; i++) {
        if (i % 2 == 0) {    
            sum = sum + i;
        }
    }

    printf("Sum of even numbers = %d\n", sum);

    return 0;
}

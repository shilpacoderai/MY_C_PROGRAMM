#include <stdio.h>

int main() {
    int l,b,area;
    printf("enter the value of l\n");
    scanf("%d",&l);
    printf("enter the value of b\n");
    scanf("%d",&b);
    area=l*b;
    printf("area=%d\n",area);
    

    return 0;
}
    

#include <stdio.h>

int main() {
   int a, b, c;
   printf("enter three number");
   scanf("%d %d %d", &a, &b, &c);
   if(a>b){
       if(a>c){
           printf("largest=%d\n", a);
       }
       else{
           printf("largest=%d\n", c);
           
        }
        else{
 
       if(b>c){
           printf("largest=%d\n",b);
        }
        else{
            printf("largest=%d\n",c);
            
        }
   }
   }

#include<stdio.h>
int main() {
    int rose[5];
    int sum=0;

    printf("Enter 5 numbers: ");
    for(int i=0;i<5;i++){
        scanf("%d", &rose[i]);
        sum = sum + rose[i];
    }

    printf("Sum = %d\n", sum);
    return 0;
}
